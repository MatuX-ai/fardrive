# ============================================================
# FarDrive 项目根级 .gitignore
# 说明：本仓库包含 marketing-site / admin-backend / mock-server
#       多个子项目，子目录各自的构建产物依赖以下规则忽略。
# ============================================================

# --- 依赖（npm / pnpm / yarn / 其它包管理器通用） ---
**/node_modules/
.pnpm-store/

# --- 构建产物 ---
**/dist/
**/build/
**/.next/
**/.nuxt/
**/.cache/
**/.turbo/

# admin-backend 的构建脚本会把 dist 同步到 marketing-site/admin/，
# 此目录是产物而非源码，必须忽略。
marketing-site/admin/

# --- 临时预览 / 调试目录（Qoder IDE 缓存 + 截屏脚本） ---
.preview/

# --- 日志 / 临时验证产物 ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
mock-check.txt

# --- 环境变量与本地覆盖 ---
.env
.env.local
.env.*.local

# --- OS 杂项 ---
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# --- IDE / 编辑器 ---
.vscode/
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# --- TypeScript / Vite 缓存 ---
*.tsbuildinfo
.vite/

# --- 覆盖率报告 ---
coverage/
.nyc_output/
